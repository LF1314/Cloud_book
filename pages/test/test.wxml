<!--pages/test/test.wxml-->
<view class='nav' wx:if="{{ishow}}" animation="{{animation}}" style='opacity:{{viewopacity}}'></view>
<view class='mask' wx:if="{{ishow}}" bindtap='change' style='opacity:{{viewopacity}}'></view>

<button class='btn' bindtap='change'>显示隐藏导航栏</button>
